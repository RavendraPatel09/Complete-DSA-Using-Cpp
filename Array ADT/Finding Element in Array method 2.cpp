#include<iostream>
using namespace std;
int main()
{
    int arr[] = {3,2,5,6,8,9,1,7,4};
    int n = sizeof(arr)/sizeof(arr[0]);
    int l = 1, h = n;
    {
    H[A[i]]++;
        if(arr[i] - i != diff)
    }
    for(i=l ;i<=h ; i++)
    {
        if(H[i] == 0)
        {
            cout << "Missing element is: " << i << endl;
        }
    }
    return 0;
}